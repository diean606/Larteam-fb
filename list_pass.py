# hallo bro :v

pw_tambahan="kontol|sayang".split("|")
ngntd=((())>(()))+((())>(()))

def awok(ngentod):
	return [ngentod,ngentod+"123",ngentod+"12345"]

def pw_list(ngentot,method=None):
	if len(ngentot)==2:
		aap_afandi=ngentot[1].split(" ")
		if len(aap_afandi[ngntd])!=ngntd:
			asu=awok(aap_afandi[ngntd].lower())
			if len(aap_afandi[ngntd]) < 6:
				del asu[ngntd]
				[asu.append(x) for x in pw_tambahan]
			else:
				[asu.append(x) for x in pw_tambahan]
		else: asu=pw_tambahan
	else: asu=pw_tambahan
	if "api" in str(method):
		[asu.append(x) for x in ["anjing","bangsat","bismillah","rahasia","Indonesia","123456","qwerty"]]
	return asu